{
  "name": "website_11ty",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prebuild": "rm -rf _site",
    "build": "npx sass assets/scss/main.scss assets/css/main.css && npx eleventy",
    "predev": "rm -rf _site",
    "dev": "npx eleventy --serve",
    "sass": "npx sass assets/scss/main.scss assets/css/main.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@11ty/eleventy-navigation": "^0.3.5"
  },
  "dependencies": {
    "sass": "^1.63.6"
  }
}
